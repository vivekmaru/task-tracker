// Package contracts defines Forge's agent-facing operation catalog.
//
// The catalog is intentionally transport-neutral. REST routes, CLI commands,
// and MCP tools should import these definitions instead of inventing separate
// names, descriptions, or JSON Schema payloads.
package contracts
